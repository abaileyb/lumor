<%= form_for [@idea, @photo], :url => idea_photos_path(@idea), :html => { :multipart => true } do |form| %>
  <%= form.file_field :image %>

  <%= form.submit :submit %>
<% end %>


<%= link_to 'Back' , idea_path(@idea) %>